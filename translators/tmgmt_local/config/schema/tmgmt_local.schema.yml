tmgmt.translator.settings.local:
  type: tmgmt.translator_base
  label: 'Local translator settings'
  mapping:
    allow_all:
      type: boolean
      label: 'Allow all'

action.configuration.task_assign_action:
  type: action_configuration_default
  label: 'Assign task configuration'
